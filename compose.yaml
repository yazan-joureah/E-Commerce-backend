version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
<<<<<<< HEAD
      MONGO_INITDB_DATABASE: ecommerce
    volumes:
      - mongodb_data:/data/db
    networks:
      - ecommerce-network  # CHANGED: Now on the main network
=======
      MONGO_INITDB_DATABASE: ecommerce  # Add default database
    volumes:
      - mongodb_data:/data/db
    networks:
      - mongo-network
    # Add this command to bind to all interfaces
>>>>>>> 0d069d1694a940c82f499278be06ae254d02fbc2
    command: ["mongod", "--bind_ip_all"]

  mongo-express:
    image: mongo-express:latest
    container_name: mongo-express
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: password
      ME_CONFIG_MONGODB_ENABLE_ADMIN: "true"
    depends_on:
      - mongo
    networks:
      - ecommerce-network  

 # image-service:
  #  build: .
  # container_name: ecommerce-image-service
  # restart: unless-stopped
  # ports:
  #    - "3000:3000"

  #  environment:
      # FIXED: Use 'mongo' as the hostname, not 'localhost'
  #    - DB_STR=mongodb://admin:password@mongo:27017/
  #    - DB_NAME=ecommerce
  #    - NODE_ENV=development
  #    - PORT=3000

  #  volumes:
      # Persistent storage for uploaded files
  #    - uploads_volume:/usr/src/app/uploads 

  #  depends_on:
  #    - mongo
  #  networks:
  #    - ecommerce-network  # This line is now correct

volumes:
  mongodb_data:
    driver: local

 # uploads_volume:  # New volume for uploads
 #   driver: local    

# DEFINE ONLY ONE NETWORK
networks:
<<<<<<< HEAD
  ecommerce-network:
=======
  mongo-network:
>>>>>>> 0d069d1694a940c82f499278be06ae254d02fbc2
    driver: bridge